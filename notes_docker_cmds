docker ps -a
docker system  prune -f  (to clean)
docker network ls

docker ps
docker network ls
cat ~/.kube/config

docker exec -it a65 bash (here a65 is the starting string of a container)
docker run <container name>
